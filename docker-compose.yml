version: '3.8'

services:
  quiz-app:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./results:/app/results
      - ./quiz_app_public.py:/app/quiz_app_public.py
    environment:
      - STREAMLIT_SERVER_HEADLESS=true
      - STREAMLIT_SERVER_PORT=8501
    restart: unless-stopped
